using UnityEngine;
using System.Collections;

public class SelfLook : MonoBehaviour {

	/*public GameObject Looker;
	public Quaternion lRotation;
	public Quaternion rotation;
	
	// Update is called once per frame
	public Quaternion UpdateMe () {
		if(Looker!=null)
		{
			lRotation = rotation;
			rotation = Looker.GetComponent<SelfLooker>().UpdateMe();
			return rotation;
		}
		else
			return rotation;
	}*/
	
	
}
